/**
 * @param {number} n
 * @return {string[]}
 */
//22. Generate Parentheses
var generateParenthesis = function(n) {
    let array = [];
    let right = [];
    while(n-- > 0) {
        left.push('(');
        right.push(')');
    }
};